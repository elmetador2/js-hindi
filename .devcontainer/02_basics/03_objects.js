//singleton 

//object literals
//Object.create// singleton method

const JsUser= {
    name:"sharique",//where name is key and sharique is the value
    age:21,
    location:"ghaizabad"
    isLoggedIn: false,
    lastLoginDays:["monday","saturday"]
 }
 console.log(JsUser.email);
 console.log(JsUser[email]);
 Object.freeze(JsUser)/we cant chnge the value of jsuser now
 [mySum]="hitesh"//its a symbol we use square brcket for symbol
 mySum="hitesh"//acts as a string
 JsUser.greeting=function () {
    console.log("hello  js user",${this.name}`);
    
 }
 console.log(JsUser.greeting);//output=function(aninymus)
 